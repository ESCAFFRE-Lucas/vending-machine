export class ChangeCalculator {
    public static calculateOptimalChange(
        amount: number,
        availableCoins: Map<number, number>
    ): number[] | null {
        // TODO: Implémenter l'algorithme glouton
        // Pour l'instant, on retourne null pour faire échouer les tests
        return null;
    }
}